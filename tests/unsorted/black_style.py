"""Fixture wrapped the way the black profile wraps a long from-import."""

from collections import (
    Counter,
    OrderedDict,
    defaultdict,
    deque,
    namedtuple,
)

CONTAINERS = (Counter, OrderedDict, defaultdict, deque, namedtuple)
